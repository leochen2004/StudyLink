# Database Configuration
# NOTE: These are default settings.
# You can override them by creating a '.env.local' file in the project root with the following keys:
# DB_URL, DB_USERNAME, DB_PASSWORD
db.url=jdbc:mysql://localhost:3306/study_link_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=123456
db.driver=com.mysql.cj.jdbc.Driver
